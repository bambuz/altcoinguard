<div class="col-md-4">
	<%= form_for [:admin, @calcoin] do |f| %>

		<div class="form-group">
			<%= f.label :name %>
			<%= f.text_field :name, class: "form-control" %>
		</div>

		<div class="form-group">
			<%= f.label :launchdate %>
			<%= f.text_field :launchdate, class: "form-control" %>
		</div>

		<div class="form-group">
			<%= f.submit "Submit", class: "btn btn-success" %>
			<%= link_to "Cancel", admin_calcoins_url, class: "btn btn-danger" %>
		</div>

	<% end %>
</div>