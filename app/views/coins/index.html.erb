<h1>Coins</h1>

<table class="table">
	<% @coins.each do |coin| %>
		<tr>
			<td><%= image_tag coin.thumbnail %> <%= coin.name %></td>
			<td><%= link_to "Coin info", coin_coin_infos_path(coin) %></td>
			<td>Pools</td>
			<td>Exchanges</td>
		</tr>
	<% end %>
</table>
